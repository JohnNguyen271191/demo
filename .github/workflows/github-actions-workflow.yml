name: GitHub Action Demo
on: push
jobs: 
  Example-Actions-Job:
    name: Exploring Github Actions
    runs-on: ubuntu-latest
    steps:
      - run: echo "The job was triggered by a ${{github.event.name}} event."
      - name: Check out repository code
        uses: actions/checkout@v2
        
        
